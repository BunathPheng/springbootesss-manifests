---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springbootesss                  # Ingress name
  labels:
    app: springbootesss
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springbootesss.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springbootesss               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
